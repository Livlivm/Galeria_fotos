name: galeria_fotos
description: Aplicativo de galeria de fotos - SENAI

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  image_picker: ^1.1.2
  gal: ^2.2.1
  path_provider: ^2.1.4
  shared_preferences: ^2.5.3
  share_plus: ^11.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  dependencies:
  photo_manager: ^3.12.0

  assets:
    - assets/icon.jpg